{
  "name": "notion-cli",
  "version": "1.0.0",
  "description": "CLI for exploring and reading content from a Notion workspace",
  "type": "module",
  "bin": {
    "notion-cli": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "cli": "npx tsx cli.ts"
  },
  "keywords": [
    "notion",
    "api",
    "cli",
    "postman"
  ],
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.10.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0"
  },
  "dependencies": {
    "commander": "^14.0.2"
  }
}
